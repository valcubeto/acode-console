{
  "name": "acode-plugin",
  "version": "1.0.0",
  "description": "A Console for Acode",
  "main": "dist/main.js",
  "repository": "https://github.com/valcubeto/acode-console.git",
  "author": "Val Cubeto",
  "license": "MIT",
  "dependencies": {
    "@types/ace": "^0.0.50",
    "html-tag-js": "^1.1.41"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "jszip": "^3.10.1",
    "@types/bun": "latest"
  },
  "scripts": {
    "dev": "tsc --noEmit && bun esbuild.config.mjs --serve",
    "build": "tsc --noEmit && bun esbuild.config.mjs"
  },
  "browserslist": "cover 100%,not android < 5",
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}